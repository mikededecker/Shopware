name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
#      - uses: actions/checkout@v1
#        with:
#          repository: shopware/shopware
#          ref: '5.6'
#          path: Shopware
      - name: Php version
        run: |
          composer create-project shopware/shopware .
